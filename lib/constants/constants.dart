import 'package:flutter/material.dart';

const prefixColor = '0xFF';
const myColor = 'ff5462f';
const secColor = '888888';
final primaryColor = int.parse((prefixColor + myColor).toString());
final secondaryColor = int.parse((prefixColor + secColor).toString());
final String appTitle = 'VirtusApp';
